import React from "react";

import { IconWrapper, IconContent } from "./styles";

const AppIcon = () => (
  <IconWrapper>
    <IconContent iconSize={100} icon="git-push" />
  </IconWrapper>
);

export default AppIcon;
